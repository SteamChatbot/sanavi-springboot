<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sanavi.backend.board.mapper.BoardMapper">

    <select id="selectBoardList" resultType="com.sanavi.backend.board.dto.BoardResponseDto">
        SELECT
            id AS board_id,
            user_id,
            nickname,
            title,
            content,
            view_count,
            created_at,
            updated_at
        FROM board
        WHERE deleted = 1
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    AND content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'writer'">
                    AND nickname LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND (
                        title LIKE CONCAT('%', #{keyword}, '%')
                        OR content LIKE CONCAT('%', #{keyword}, '%')
                        OR nickname LIKE CONCAT('%', #{keyword}, '%')
                    )
                </otherwise>
            </choose>
        </if>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectBoardCount" resultType="int">
        SELECT COUNT(*)
        FROM board
        WHERE deleted = 1
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    AND content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'writer'">
                    AND nickname LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND (
                        title LIKE CONCAT('%', #{keyword}, '%')
                        OR content LIKE CONCAT('%', #{keyword}, '%')
                        OR nickname LIKE CONCAT('%', #{keyword}, '%')
                    )
                </otherwise>
            </choose>
        </if>
    </select>

    <select id="selectBoardById" resultType="com.sanavi.backend.board.dto.BoardResponseDto">
        SELECT
            id AS board_id,
            user_id,
            nickname,
            title,
            content,
            view_count,
            created_at,
            updated_at
        FROM board
        WHERE id = #{boardId}
        AND deleted = 1
    </select>

    <insert id="insertBoard" 
            parameterType="com.sanavi.backend.board.dto.BoardRequestDto"
            useGeneratedKeys="true"
            keyProperty="boardId"
            keyColumn="id">
        INSERT INTO board (
            user_id,
            nickname,
            title,
            content
        ) VALUES (
            #{userId},
            #{nickname},
            #{title},
            #{content}
        )
    </insert>

    <update id="updateBoard">
        UPDATE board
        SET
            title = #{board.title},
            content = #{board.content}
        WHERE id = #{boardId}
        AND deleted = 1
    </update>

    <update id="deleteBoard">
        UPDATE board
        SET deleted = 0
        WHERE id = #{boardId}
        AND deleted = 1
    </update>

    <update id="increaseViewCount">
        UPDATE board
        SET view_count = view_count + 1
        WHERE id = #{id}
        AND deleted = 1
    </update>
</mapper>