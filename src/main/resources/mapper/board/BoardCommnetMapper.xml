<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sanavi.backend.board.mapper.BoardCommentMapper">

    <select id="selectCommentsByBoardId"
            resultType="com.sanavi.backend.board.dto.BoardCommentResponseDto">
        SELECT
            id AS commentId,
            board_id AS boardId,
            user_id AS userId,
            nickname AS nickname,
            content AS content
        FROM board_comment
        WHERE board_id = #{boardId}
          AND deleted = 1
        ORDER BY id ASC
    </select>

    <insert id="insertComment">
        INSERT INTO board_comment (
            board_id,
            user_id,
            nickname,
            content
        ) VALUES (
            #{boardId},
            #{request.userId},
            #{request.nickname},
            #{request.content}
        )
    </insert>

    <update id="softDeleteComment">
        UPDATE board_comment
        SET deleted = 0
        WHERE board_id = #{boardId}
          AND id = #{commentId}
          AND user_id = #{userId}
          AND deleted = 1
    </update>

</mapper>