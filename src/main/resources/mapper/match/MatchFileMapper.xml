<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sanavi.backend.match.mapper.MatchFileMapper">

    <select id="selectFileById" resultType="com.sanavi.backend.match.dto.MatchFileDto">
        SELECT
            id            AS fileId,
            match_id      AS matchId,
            original_name AS originalName,
            saved_name    AS savedName,
            file_path     AS filePath,
            file_size     AS fileSize,
            file_type     AS fileType,
            created_at    AS createdAt
        FROM match_file
        WHERE id = #{fileId}
          AND deleted = 1
    </select>

    <select id="selectFilesByMatchId" resultType="com.sanavi.backend.match.dto.MatchFileDto">
        SELECT
            id            AS fileId,
            match_id      AS matchId,
            original_name AS originalName,
            saved_name    AS savedName,
            file_path     AS filePath,
            file_size     AS fileSize,
            file_type     AS fileType,
            created_at    AS createdAt
        FROM match_file
        WHERE match_id = #{matchId}
          AND deleted = 1
    </select>

    <insert id="insertFile" parameterType="com.sanavi.backend.match.dto.MatchFileDto"
            useGeneratedKeys="true" keyProperty="fileId">
        INSERT INTO match_file (
            match_id,
            original_name,
            saved_name,
            file_path,
            file_size,
            file_type,
            created_at
        ) VALUES (
            #{matchId},
            #{originalName},
            #{savedName},
            #{filePath},
            #{fileSize},
            #{fileType},
            NOW()
        )
    </insert>

    <update id="deleteFilesByMatchId">
        UPDATE match_file
        SET deleted = 0
        WHERE match_id = #{matchId}
          AND deleted = 1
    </update>

    <update id="deleteFileById">
        UPDATE match_file
        SET deleted = 0
        WHERE id = #{fileId}
          AND deleted = 1
    </update>

</mapper>
